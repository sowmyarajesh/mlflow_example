# Get Started with ML flow

## Step -1 Set up the project for learning the use and workflow of MLflow.

1. In the terminal on the root directory of this repo, check if python virtual environment named `.venv` is created. If no, create the virtual environment named `.venv`
2. Activate the virtual environment.
3. install libraries from requirements.txt


## Step -2 Set up mlflow
